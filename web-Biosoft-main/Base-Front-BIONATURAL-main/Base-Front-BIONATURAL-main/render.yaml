services:
  - type: web
    runtime: static
    name: bionatural-frontend
    buildCommand: npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
